<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.skyj">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.camera2" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_main"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_main_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Post.ApplyActivity"></activity>
        <activity android:name=".write.Write6Activity" />
        <activity android:name=".write.Write5Activity" />
        <activity android:name=".write.Write4Activity" />
        <activity android:name=".write.Write3Activity" />
        <activity android:name=".write.Write2Activity" />
        <activity android:name=".Post.PostInfoActivity" />
        <activity android:name=".write.WriteActivity" />
        <activity android:name=".Auth.JoinInfoActivity" />
        <activity android:name=".Auth.MyPageActivity" />
        <activity android:name=".Auth.JoinActivity" />
        <activity android:name=".Auth.LoginActivity" />
        <activity android:name=".StudyActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="android.max_aspect"
            android:value="2.1" />
    </application>

</manifest>